# Dependencies
/node_modules
.yarn/cache
.yarn/install-state.gz
.yarn/build-state.yml
.yarn/unplugged
.pnp.*

# Production build
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store

# Secrets / env (PUBLIC repo — never track; keep .env.example template)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local
*.jks
*.keystore
key.properties
keystore.properties
*service-account*.json
.npmrc

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Firebase local state (keep firebase.json + .firebaserc, ignore runtime state)
.firebase/
firebase-debug.log
firebase-debug.*.log
.runtimeconfig.json

# Lockfiles other than yarn (per workspace policy)
package-lock.json
pnpm-lock.yaml

# Local-only ignore patterns (the underscore-prefixed _planning/ folder STAYS in git)
*.ignore.*
project-record-ignore/
